class una{
   constructor(a: number);
    constructor(a) {
        
    }
    hola(a: string|boolean):string;
    hola(b: number):boolean;
    hola(b):string|boolean|number {
         return false;
    }
}
//Intentamos ver como hacerlo funcionar para convencer de que no se puede